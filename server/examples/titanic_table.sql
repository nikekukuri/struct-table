create table titanic_table (
	passenger_id int
	, survived int
	, pclass int
	, name varchar(512)
	, sex varchar(512)
	, age float
	, sibsp int
	, parch int
	, ticket varchar(512)
	, fare float
	, cabin varchar(512)
	, embarked varchar(512)
);
